import LegalDocumentPage from "./LegalDocumentPage";
import { enterpriseTerms } from "@/content/legal";

const TermsPage = () => <LegalDocumentPage document={enterpriseTerms} />;

export default TermsPage;
